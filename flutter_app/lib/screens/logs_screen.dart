import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'package:intl/intl.dart';
import '../main.dart';
import '../models/log_entry.dart';
class LogsScreen extends StatelessWidget { const LogsScreen({super.key}); @override Widget build(BuildContext context) { final state = context.watch<AppState>(); return Padding(padding: const EdgeInsets.all(32), child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text('SYSTEM / EVENTS', style: Theme.of(context).textTheme.labelSmall?.copyWith(color: const Color(0xFF00D97E), letterSpacing: 1.5)), const SizedBox(height: 8), Text('Logs', style: Theme.of(context).textTheme.headlineMedium?.copyWith(fontWeight: FontWeight.w800))]), OutlinedButton.icon(onPressed: state.exportLogs, icon: const Icon(Icons.download_outlined), label: const Text('Export'))]), const SizedBox(height: 24), Expanded(child: Container(width: double.infinity, padding: const EdgeInsets.all(18), color: Theme.of(context).colorScheme.surface, child: ListView(children: state.logs.entries.map((entry) => Text('${DateFormat('HH:mm:ss').format(entry.timestamp)}  ${entry.message}', style: TextStyle(fontFamily: 'monospace', color: entry.level == LogLevel.error ? Colors.redAccent : null, height: 1.8))).toList())))])); } }
